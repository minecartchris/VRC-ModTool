# Web server only — no audio capture stack, so this installs cleanly on Linux.
# Use it when hosting the server somewhere other than your gaming PC:
#     pip install -r requirements-web.txt && python run_web.py
requests
cryptography
fastapi
uvicorn[standard]
jinja2
python-multipart
