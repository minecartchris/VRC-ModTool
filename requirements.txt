# Python 3.10+ on Windows.
#
# Offline speech-to-text also needs a Vosk model in this folder, e.g.
# vosk-model-small-en-us-0.15 (small/fast) or vosk-model-en-us-0.22 (large/accurate),
# downloaded from https://alphacephei.com/vosk/models and unzipped here.

numpy
scipy
requests
vosk
PyAudioWPatch
Pillow

# ---- web server (run_web.py) ----
# Only needed to run the web front end; the desktop app works without them.
# On a Linux/VPS deployment install just these — see requirements-web.txt.
fastapi
uvicorn[standard]
jinja2
python-multipart
